services:
  - type: web
    name: ivie-ai-proxy
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    plan: free  # Free tier is fine since we're just proxying
    region: oregon
    healthCheckPath: /health